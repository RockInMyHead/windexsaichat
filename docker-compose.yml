services:
  windexai:
    build: .
    ports:
      - "8003:8003"
    environment:
      - DATABASE_URL=sqlite:///./windexai.db
      - WINDEXAI_API_KEY=${WINDEXAI_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - .:/app

    build: .
    ports:
      - "8003:8003"
    environment:
      - DATABASE_URL=sqlite:///./windexai.db
      - WINDEXAI_API_KEY=${WINDEXAI_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - .:/app
